before_script:          # install the virtual environment
  - pipenv install --dev --pre
  - pipenv shell
  - which python

stages:
  - dep-check
  - quality
  - test
  - code-coverage

pipenv-install:
  stage: dep-check
  script:
    - which python
    - pipenv check

flake8:
  stage: quality
  script:
    - flake8

black:
  stage: quality
  script:
    - black

mypy:
  stage: quality
  script:
    - echo TODO

test:
  stage: test
  script:
    - pytest pymint/

test-code-coverage:
  stage: code-coverage
  script:
    - echo TODO
